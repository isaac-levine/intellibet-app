import React from "react";

const Logo = ({ ...props }) => (
  <div
    style={{ display: "flex", alignItems: "center", justifyContent: "center" }}
  >
    <h1>intellibet.io</h1>
  </div>
);

export default Logo;
